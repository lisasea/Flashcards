extends layout.pug

block content
    section#content 
        ul
          each color in colors 
            li= color 
        h2= text
        if hint
            p
                i Hint: #{hint}
        a(href=`${id}?side=${sideToShow}`)= sideToShowDisplay
        br 
        a(href='/cards') Next card